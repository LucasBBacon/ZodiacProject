using System.Collections;
using UnityEngine;

[CreateAssetMenu(fileName = "EntitiyData", menuName = "EntitiyData", order = 0)]
public class EntityData : ScriptableObject
{
    public float moveSpeed;
}